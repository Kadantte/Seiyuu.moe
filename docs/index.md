# Documentation Index

This directory contains durable project knowledge that should guide future code and agent work.

## Start Here

- `PROJECT.md` explains what Seiyuu.moe does, the main user-facing features, and the shared terminology used across the frontend and backend.
- `ARCHITECTURE.md` maps the repository layout, backend project boundaries, frontend structure, and test layers.

## Maintenance

Keep these docs small and tied to real work. Update them when behavior, architecture, setup commands, or documented workflows change. Avoid adding placeholder docs for future categories until the repo has repeated friction that justifies them.
